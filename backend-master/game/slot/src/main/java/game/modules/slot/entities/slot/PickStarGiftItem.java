package game.modules.slot.entities.slot;

public enum PickStarGiftItem {
     KEY("KEY", 0),
     BOX("BOX", 1),
     GOLD("GOLD", 2),
     LOSE("LOSE", 3);

     private PickStarGiftItem(String s, int n) {
     }
}
